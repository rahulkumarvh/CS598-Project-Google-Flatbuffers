namespace CS598; 

enum DataType: byte { Int, Float, String } 

table Meta { 
    name: string; 
    dtype: DataType; 
} 

table Column { 
    metadata: Meta; 
    int_values: [int64]; 
    float_values: [float64]; 
    string_values: [string]; 
} 

table DataFrame { 
    metadata: string; 
    columns: [Column]; 
} 

root_type DataFrame; 